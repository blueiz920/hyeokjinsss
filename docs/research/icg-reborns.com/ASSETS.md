# ICG Reborns project-end assets

- No raster image, video, background image, canvas, Lottie, or font file is required for this bounded component.
- The source next-project arrow is an inline SVG (`24 × 24` viewBox, rendered `46 × 46`).
- The local component already uses a text arrow and Pretendard. Per the user-approved adaptation, no source SVG or source font is copied.
- Source screenshots are stored under `.superloopy/evidence/website-clone/icg-reborns.com/source/`.
